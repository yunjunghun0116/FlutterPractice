import 'package:flutter/material.dart';

class MainRiderScreen extends StatelessWidget {
  const MainRiderScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Text('라이더지원'),
    );
  }
}

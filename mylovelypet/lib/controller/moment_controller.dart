import 'package:get/get.dart';

class MomentController extends GetxController{
  //TODO moment업로드 진행 후에 여기서 새로이 불러주기위해서는 여기서 가져와야하고
  //TODO obs를 통해서, 그리고 reload를 통해서 메인스크린에서 유동적으로 화면에 띄워준다
  //여기에서는 List<PetMoment(model)>의 형태로 갖고있다가 필요시 모든것들을 리턴해주는 방식으로 진행해주면
  //관리하기가 편할것같다
}
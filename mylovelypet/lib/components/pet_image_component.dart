import 'package:flutter/material.dart';

class PetImageComponent extends StatelessWidget {

  final String imageUrl;

  PetImageComponent(this.imageUrl);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
